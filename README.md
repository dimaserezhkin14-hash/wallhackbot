# Wallhack Pro — final smooth engine

GitHub Pages ready. Upload `index.html`, `engine.worker.js`, and `README.md` to the repository root. The real engine runs in a Web Worker; the original hint/apply-move flow remains active.
